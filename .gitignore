node_modules/
.env
secrets/*.txt
!secrets/.gitkeep
data/
*.log
music/*.mp3
!music/README.md
!music/library.json
Input/

# Per-machine deploy state: which commit is actually built into the container.
.deployed_sha

# Colour LUTs are third-party licensed assets — mounted, never committed (see luts/README.md).
luts/*.cube
